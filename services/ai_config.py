"""Единая точка настройки модели DeepSeek.

Раньше строка "deepseek-chat" была захардкожена в трёх разных файлах
(deepseek_service.py, text_handler.py, main.py). Если DeepSeek снова
переименует или отключит модель, придётся править в одном месте —
здесь, либо просто задать переменную окружения DEEPSEEK_MODEL в
настройках хостинга (Railway Variables и т.п.), не трогая код.

СТАТУС НА 12.09.2026 (проверено по официальной документации
https://api-docs.deepseek.com/): "deepseek-chat" и "deepseek-reasoner"
полностью отключены с 24.07.2026 — вызовы с этими именами возвращают
ошибку. "deepseek-v4-flash" тоже официально снят с производства, но
пока временно продолжает принимать запросы (роутится на deepseek-flash
по той же цене) — полагаться на это не стоит. Актуальное имя быстрой
модели — "deepseek-flash" (DeepSeek-V4.1-Flash). Если бот вообще не
отвечает на сообщения — в первую очередь проверь, не осталось ли в
переменных окружения хостинга старое имя "deepseek-chat".

DeepSeek снова может переименовать модель. Перед каждым деплоем стоит
свериться с https://api-docs.deepseek.com/quick_start/pricing/ и при
необходимости задать DEEPSEEK_MODEL в окружении, не редактируя код.
"""

import os

DEEPSEEK_MODEL = os.environ.get("DEEPSEEK_MODEL", "deepseek-flash")

# "Режим размышления" (thinking) — модель сначала рассуждает пошагово,
# потом отвечает. Полезно для многошаговых/неоднозначных запросов
# (например "напомни дважды в день в течение двух недель"), но дороже
# и медленнее для обычных "записала покупку". low/high/max — по
# документации DeepSeek: low для простых задач, high для ежедневных
# агентных сценариев, max для сложных. "disabled" — выключить совсем.
DEEPSEEK_THINKING_EFFORT = os.environ.get("DEEPSEEK_THINKING_EFFORT", "high").strip().lower()


